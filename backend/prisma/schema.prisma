generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Tarefas {
  id        Int      @id @default(autoincrement())
  nome      String
  inicio    DateTime
  fim       DateTime
  descricao String
}